// TODO: replace with the ui-kit's own "sortable column indicator (up+down chevrons)" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `ChevronsUpDown`.
export { ChevronsUpDown } from "lucide-react"
