// TODO: replace with the ui-kit's own "checked/selected" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `Check`.
export { Check } from "lucide-react"
