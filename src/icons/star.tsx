// TODO: replace with the ui-kit's own "rating" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `Star`.
export { Star } from "lucide-react"
