// TODO: replace with the ui-kit's own "chevron left" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `ChevronLeft`.
export { ChevronLeft } from "lucide-react"
