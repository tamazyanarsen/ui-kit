/* Строительные леса демо-страницы, а не часть ui-kit.
   Дизайн-чек №7: в Storybook висел компонент «Accordion (нет аналога в Figma)»
   — его в продукте нет, и в каталоге кита ему не место. Реализация при этом
   нужна: на ней собрана вся демо-страница (App.tsx и src/demo/*), где каждый
   компонент лежит в раскрывающейся секции. Поэтому компонент убран из
   публичного экспорта и из Storybook, а сам файл переехал сюда — так видно,
   что это внутренняя обвязка демо. */
export { Accordion, AccordionItem, AccordionTrigger, AccordionPanel } from "./accordion"
