// TODO: replace with the ui-kit's own "pending/time status" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `Clock`.
export { Clock } from "lucide-react"
