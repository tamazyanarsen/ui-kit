// TODO: replace with the ui-kit's own "show password" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `Eye`.
export { Eye } from "lucide-react"
