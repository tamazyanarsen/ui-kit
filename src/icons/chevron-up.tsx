// TODO: replace with the ui-kit's own "chevron up" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `ChevronUp`.
export { ChevronUp } from "lucide-react"
