// TODO: replace with the ui-kit's own "add/upload" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `CirclePlus`.
export { CirclePlus } from "lucide-react"
