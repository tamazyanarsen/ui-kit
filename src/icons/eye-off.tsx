// TODO: replace with the ui-kit's own "hide password" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `EyeOff`.
export { EyeOff } from "lucide-react"
