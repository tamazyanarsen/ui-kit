// TODO: replace with the ui-kit's own "calendar/date" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `CalendarDays`.
export { CalendarDays } from "lucide-react"
