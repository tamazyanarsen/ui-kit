// TODO: replace with the ui-kit's own "locked field" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `Lock`.
export { Lock } from "lucide-react"
