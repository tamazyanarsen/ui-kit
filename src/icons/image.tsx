// TODO: replace with the ui-kit's own "image placeholder" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `ImageIcon`.
export { ImageIcon } from "lucide-react"
