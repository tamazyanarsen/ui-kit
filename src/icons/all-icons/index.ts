/* ------------------------------------------------------------------------
   Полный набор кита — дизайн-чек №3 №10: «Набор иконок не исчерпывающий и не
   целевой. Нужны все иконки из кита в двух размерах, и именно их
   централизованно нужно использовать в компонентах кита».

   Здесь — все иконки страницы ALL ICONS (канвас 70326:26), категории 01–22,
   ровно те, на которые дизайнер дал ссылки. Каждая пришла двумя отдельными
   начертаниями мастера (16x16 и 24x24), переключение — пропом `size`, как у
   канонических иконок кита (см. `../kit.ts`). Файлы собраны скриптом из
   SVG-выгрузки листов категорий, а не перерисованы руками.

   У большинства глифов координаты пересчитаны в 0..16 / 0..24. Там, где
   внутри маска, clipPath или собственный поворот узла, содержимое оставлено
   как есть, а рамка задана через viewBox — так кроп верен при любой
   вложенности трансформаций.

   Имена — kebab-case от имени слоя в Figma, поэтому искать иконку можно
   ровно по тому названию, которое дизайнер видит в макете. Несколько глифов
   существуют в двух названиях: в `../kit.ts` — каноническое имя кита (`X`,
   `Ellipsis`, `Check`, …), здесь — фигмовское (`CloseCross`, `More`, `Mark`,
   …). Это не дубли по недосмотру: компоненты кита ходят по первым, дизайнер
   ищет по вторым.

   Список разрезан на три алфавитных куска — иначе один файл переваливает за
   предел в 300 строк. Границы: A–F, G–P, Q–Z.
   --------------------------------------------------------------------- */
export * from "./a-f"
export * from "./g-p"
export * from "./q-z"
