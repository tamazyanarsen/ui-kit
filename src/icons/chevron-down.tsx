// TODO: replace with the ui-kit's own "chevron down" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `ChevronDown`.
export { ChevronDown } from "lucide-react"
