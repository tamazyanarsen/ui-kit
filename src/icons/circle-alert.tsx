// TODO: replace with the ui-kit's own "error/warning status" icon (Figma export).
// Delete the re-export below and paste the real <svg>...</svg> markup here
// as a component — keep the export name `CircleAlert`.
export { CircleAlert } from "lucide-react"
